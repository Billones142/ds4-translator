# Compiled binaries
build/
ds4-translator
ds4-ctl
src/*.o
*.so

# Generated by the release workflow as a fallback for `make` when building
# from an extracted tarball with no .git directory; never hand-edit/commit.
VERSION

# Local context / operational files
.sober/
.agents/
